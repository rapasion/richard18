---
 - name: Ensure Curl Download Path Exist
   file:
     path: "{{ gml_source_path }}/download"
     state: directory
     mode: 0750
     owner: "{{ code_user }}"
     group: "{{ code_user }}"
   tags:
    - curl
