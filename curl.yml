---
 - name: Ensure Curl Download Path Exist
   file:
     path: "{{ curl_source_clone_dir }}"
     state: directory
     mode: 0750
     owner: "{{ code_user }}"
     group: "{{ code_user }}"
   tags:
    - curl
